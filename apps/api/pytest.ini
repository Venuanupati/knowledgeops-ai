[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    --strict-markers

markers =
    unit: fast tests that do not require external services
    integration: tests that require Postgres, Qdrant, or other services